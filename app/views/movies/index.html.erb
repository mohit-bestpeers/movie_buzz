<main>
  <section class="featured-movies">
    <h2>Movies</h2>
    <div class="movie-grid margin">
    <% @movies.each do |movie| %>
      <div class="movie">
        <%=image_tag movie.image ,class: "style_image" %> 
        <h5 class="card-title">Movie : <%=movie.name%></h5>
        <h6><p class="card-text">Type :<%= movie.category.name %></p></h6>
        <h6><p class="card-text">Rating :<%=movie.rating%><span class="fa fa-star checked"></p></h6>
        <h6><p class="card-text">Released_on :<%=movie.released_on%></p></h6>
        <%= link_to 'Details', movie %>
      </div>
    <% end %>
    </div>
    <%= will_paginate @clients,previous_label:'&laquo;',next_label: '&raquo;',list_classes: %w(pagination justify-content-end) %> 
  </section>

  <section class="featured-movies">
    <h2>Feature Movies</h2>
    <div class="movie-grid margin">
    <% @movies.each do |movie| %>
      <% if movie.released_on > Date.today %>
      <div class="movie">
        <%=image_tag movie.image, class: "style_image" %> 
        <h5 class="card-title">Movie : <%=movie.name%></h5>
        <h6><p class="card-text">Released_on :<%=movie.released_on%></p></h6>
        <%= link_to 'Details', movie %>
      </div>
    <% end %>
    <% end %>
    </div>
    <%= will_paginate @Movies,previous_label:'&laquo;',next_label: '&raquo;',list_classes: %w(pagination justify-content-end) %> 
  </section>

  <section class="featured-movies">
    <h2>Popular</h2>
    <div class="movie-grid margin">
    <% @movies.each do |movie| %>
    <% if movie.rating != nil && movie.rating > 3.5 %>
      <div class="movie">
        <%=image_tag movie.image, class: "style_image" %> 
        <h5 class="card-title">Movie : <%=movie.name%></h5>
        <h6><p class="card-text">Rating :<%=movie.rating%><span class="fa fa-star checked"></p></h6>
        <h6><p class="card-text">Released_on :<%=movie.released_on%></p></h6>
        <%= link_to 'Details', movie %>
      </div>
    <% end %>
     <% end %>
    </div>
    <%= will_paginate @clients,previous_label:'&laquo;',next_label: '&raquo;',list_classes: %w(pagination justify-content-end) %> 
  </section>

</main>

  <footer>
    <!-- Footer content -->
  </footer>
<% if user_signed_in? && current_user.admin? %>
<%= link_to "New Movie", new_movie_path %>
<% end %>



 









